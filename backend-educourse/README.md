# EduCourse Backend API

## Konfigurasi Environment (.env)
Untuk menjalankan proyek ini di lokal, buat file `.env` pada direktori utama dan isi dengan variabel berikut:

PORT=3000
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=educourse